## Nexus Wallet

This app was created using https://delidev.com.
Need help or want to join the community? Join our [Discord](https://discord.gg/shDEGBSe2d).

To run the devserver:
```
pnpm install
pnpm run dev
```
pnpx wrangler types = for tsconfig.worker.json error to create the worker-configutation.d.ts file

# Install jsonwebtoken and its types
pnpm install jsonwebtoken
pnpm install -D @types/jsonwebtoken
pnpm install -D @cloudflare/workers-types
